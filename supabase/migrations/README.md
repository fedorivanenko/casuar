# Casuar database migrations

Canonical database: Supabase `casuar-core` (`itsszsqxjqzaecqlbidk`).

Casuar now uses only the `casuar_do` causal-model / autoresearch / Wind Tunnel schema. The older public semantic prototype (`objects`, `claims`, `research_projects`, etc.) is intentionally retired and is not part of the fresh database.
